/**
 * The contract for a instance that logs
 *
 * @typedef {Object} Observer
 * @interface
 * @property {Observer~dispatcher} dispatch
 */

/**
 * @callback Observer~dispatcher
 * @param {Session} session
 * @param {string} event
 */
